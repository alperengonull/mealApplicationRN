import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    container: {
        backgroundColor: 'red',
        borderRadius: 8,
        alignItems: 'center',
        justifyContent: 'center',
        margin: 10,
        height: 40,
        width: 150
    },
    title: {
        color: 'white',
        fontSize: 15,
        fontWeight: 'bold'
    }
});
